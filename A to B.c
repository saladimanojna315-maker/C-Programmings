#include<stdio.h>
int main() 
{
    int A, B, i;
    if(scanf("%d %d", &A, &B)!=2);
    for(i = A; i <= B; i++)
    {
    printf("%d ", i);
    {
        if(i < B)
        printf("");
    }
    }
    printf("\t");
    return 0;
}

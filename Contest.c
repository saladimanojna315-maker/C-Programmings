#include<stdio.h>
int main()
{
    int X, A, B;
    int Y;
    if(scanf("%d %d %d", &X, &A, &B)!=3);
    Y=((A*1) + (B*2));
    if( Y >= X)
    printf("Qualify
");
    else
    printf("Not Qualify
");
    return 0;
}

#include<stdio.h>
int main()
{
    int X;
    int Y;
    int Z;
    scanf("%d", &X);
    scanf("%d", &Y);
    Z = Y * 30;
    if(X >= Z)
    printf("YES
");
    else
    printf("NO
");
    return 0;
}
